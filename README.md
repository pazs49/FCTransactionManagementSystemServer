	1. Install ruby and ruby on rails https://guides.rubyonrails.org/install_ruby_on_rails.html
	2. Clone the repo
	3. run "bundle install" on the terminal
    4. run "rails server" to start the server

